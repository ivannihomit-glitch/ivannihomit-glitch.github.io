* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: linear-gradient(145deg, #e0eef2 0%, #c2dbdf 100%);
    min-height: 100vh;
    padding: 20px 16px;
    font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, Helvetica, sans-serif;
}

.tracker-wrapper {
    max-width: 1280px;
    margin: 0 auto;
}

.glass-card {
    background: rgba(255, 255, 255, 0.97);
    border-radius: 48px;
    box-shadow: 0 25px 45px -12px rgba(0, 0, 0, 0.3), 0 1px 2px rgba(0,0,0,0.05);
    overflow: hidden;
    transition: all 0.2s ease;
}

.hero-section {
    background: linear-gradient(115deg, #1f4e3f 0%, #2a6b56 100%);
    padding: 28px 28px 32px 28px;
    color: white;
}

.hero-section h1 {
    font-size: 1.9rem;
    letter-spacing: -0.3px;
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
    font-weight: 700;
}

.badge {
    background: #ffb347;
    font-size: 0.85rem;
    padding: 4px 14px;
    border-radius: 100px;
    color: #1f4e3f;
    font-weight: 600;
}

.hero-section p {
    margin-top: 12px;
    opacity: 0.88;
    font-size: 1rem;
    max-width: 85%;
}

.two-columns {
    display: flex;
    flex-wrap: wrap;
}

.form-panel {
    flex: 1.2;
    min-width: 280px;
    background: #fefcf7;
    padding: 28px 24px;
    border-right: 1px solid #e7e2d4;
}

.result-panel {
    flex: 1;
    background: #ffffff;
    padding: 28px 24px;
}

/* стили формы */
.input-field {
    margin-bottom: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 8px;
}

label {
    font-weight: 600;
    color: #2b5d4a;
    font-size: 0.9rem;
    display: flex;
    align-items: center;
    gap: 8px;
}

input, select {
    width: 100%;
    padding: 14px 18px;
    border: 1.5px solid #e2dccc;
    border-radius: 34px;
    font-size: 1rem;
    background: white;
    transition: 0.2s;
    outline: none;
    font-family: inherit;
}

input:focus, select:focus {
    border-color: #2a6b56;
    box-shadow: 0 0 0 3px rgba(42, 107, 86, 0.2);
}

.radio-group {
    display: flex;
    gap: 28px;
    background: #f3f1ea;
    padding: 10px 20px;
    border-radius: 48px;
    flex-wrap: wrap;
}

.radio-group div {
    display: flex;
    align-items: center;
    gap: 8px;
}

.radio-group input {
    width: auto;
    transform: scale(1.1);
    margin: 0;
}

.radio-group label {
    font-weight: normal;
}

button {
    background: #ff9142;
    width: 100%;
    border: none;
    padding: 15px 18px;
    border-radius: 44px;
    font-weight: bold;
    font-size: 1.1rem;
    color: white;
    cursor: pointer;
    transition: all 0.2s;
    margin-top: 10px;
    box-shadow: 0 6px 14px rgba(255, 145, 66, 0.3);
}

button:hover {
    background: #e27e38;
    transform: translateY(-2px);
}

button:active {
    transform: translateY(1px);
}

/* результат */
.kcal-circle {
    background: #eef5f2;
    border-radius: 32px;
    padding: 20px;
    text-align: center;
    margin-bottom: 24px;
}

.kcal-number {
    font-size: 3.4rem;
    font-weight: 800;
    color: #1f6e56;
    line-height: 1;
}

.advice-type {
    background: #fff2e6;
    border-left: 6px solid #ff9142;
    padding: 18px;
    border-radius: 28px;
    margin: 20px 0;
}

.workout-tip {
    background: #f7f8fc;
    border-radius: 24px;
    padding: 16px 18px;
    margin-bottom: 14px;
    border: 1px solid #eee8dd;
    transition: 0.1s;
}

h3 {
    font-size: 1.3rem;
    margin-bottom: 12px;
    color: #1f4e3f;
}

h4 {
    font-size: 1rem;
    margin: 8px 0 4px;
    color: #2c5a4b;
}

hr {
    margin: 16px 0;
    border: 0;
    height: 1px;
    background: #e7e2d4;
}

.small-note {
    font-size: 0.7rem;
    text-align: center;
    color: #8f8a7a;
    margin-top: 20px;
}

.footer-meta {
    text-align: center;
    font-size: 0.7rem;
    color: #5e6f68;
    margin-top: 20px;
}

.highlight {
    font-weight: 700;
    color: #ff9142;
}

@media (max-width: 780px) {
    body {
        padding: 12px;
    }
    .form-panel {
        border-right: none;
        border-bottom: 1px solid #e7e2d4;
    }
    .hero-section h1 {
        font-size: 1.5rem;
    }
    .hero-section p {
        max-width: 100%;
    }
    .two-columns {
        flex-direction: column;
    }
    .radio-group {
        gap: 16px;
    }
}

@media (min-width: 1024px) {
    .form-panel {
        padding: 36px 32px;
    }
    .result-panel {
        padding: 36px 32px;
    }
}